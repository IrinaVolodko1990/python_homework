#2 Торговая фирма в первый день работы реализовала товаров на P тыс. руб.,
# а затем ежедневно увеличивала выручку на 3%. Какой будет выручка фирмы в тот день,
# когда она впервые превысит заданное значение Q? Сколько дней придется торговать фирме
# для достижения этого результата?
p = float(input("Введите выручку P за первый день продаж (тыс.руб.): \n"))
# p=23.45
q=float(input("Введите значение Q для расчёта условий задачи (тыс.руб.): \n"))
#q=345.45
day = 1
if p<q:
    while p<q:
        p+=p*0.03
        day+=1
    print ("Выручка P превысила значение Q после "+str(day)+ " дня продаж.\nОна составила "+ str(round(p,6))+" тыс.руб.")
else:
    print ("Введённые значения противоречат условию задачи, выручка за "
           "первый день должна быть меньше значения Q")
