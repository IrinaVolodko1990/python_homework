#Реализуйте рекурсивную функцию нарезания прямоугольника с
# заданными пользователем сторонами a и b на квадраты
# с наибольшей возможной на каждом этапе стороной.
# Выведите длины ребер получаемых квадратов и кол-во полученных квадратов.


def cutAB(a,b,qty=0):
    if a==0 or b==0:
        return ("Полученное число квадратов: \n"+str(qty))
    elif a<b:
        print ("Ребро квадрата: \n"+str(a))
        return cutAB(a,b-a,qty+1)
    print ("Ребро квадрата: \n"+str(b))
    return cutAB(a-b,b,qty+1)

a=int(input("Введите сторону 'a' прямоугольника:\n"))
b=int(input("Введите сторону 'b' прямоугольника:\n"))
print(cutAB(a,b))

